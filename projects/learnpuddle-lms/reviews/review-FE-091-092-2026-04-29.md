---
tags: [review, fe/091, fe/092, verdict/request-changes, reviewer/lp-reviewer]
created: 2026-04-29
---

# Review: FE-091 / FE-092

## Verdict: REQUEST_CHANGES

## Summary
Two final coverage-completion test suites: SignupPage (44 tests, multi-step wizard) and SecuritySettings (42 tests, 2FA + SSO). 86 claimed = 86 actual. SignupPage is fully verified including the explicit `confirm_password` exclusion and dual-format error handling. SecuritySettings has a coverage gap relative to the request narrative: the request claimed an SSO Unlink test calling `api.post('/users/auth/sso/unlink/', ...)`, but only the button-renders test exists — the API-call assertion is missing. Add it and resubmit.

## Verification

| File | Claimed | Actual | Status |
|------|---------|--------|--------|
| `pages/onboarding/SignupPage.test.tsx` | 44 | 44 | ✅ exact |
| `pages/settings/SecuritySettings.test.tsx` | 42 | 42 | ✅ exact |
| **Total** | **86** | **86** | ✅ |

### Anti-pattern scan (clean)
- No `it.skip` / `xit` / `it.todo`
- No `expect(true).toBe(true)`
- No `as any` in test setup
- All `it()` blocks contain assertions

### SignupPage behavioral checks ✅
- `confirm_password` excluded from POST payload (line 518) — explicit `not.toHaveProperty` verified
- Subdomain debounce at 500ms with 3+ char names — uses real timers and asserts on the debounced GET (lines 230-288)
- Both string-format and array-format server errors tested (lines 579, 602-603)
- QueryClient: `staleTime: Infinity, retry: false` (lines 40-42) ✅

### SecuritySettings behavioral checks
- 2FA Enable multi-step flow: setup POST (line 318) → QR + secret (322-345) → verify POST with 6-digit code (389) → backup codes modal (407) ✅
- 2FA Disable modal requires 6-digit code AND password (lines 501-503) ✅
- 3 GET endpoints fetched on mount: `/users/auth/2fa/status/`, `/users/auth/sso/status/`, `/users/auth/sso/providers/` (lines 658-676) ✅

## Critical Issues
None.

## Major Issues

### M1 — Missing SSO Unlink API-call assertion in SecuritySettings
**File:** `pages/settings/SecuritySettings.test.tsx:637-642`

The request narrative explicitly claims:
> Google provider shown linked (`is_linked: true`) with "Unlink" button calling `api.post('/users/auth/sso/unlink/', {...})`

But only this exists:
```tsx
it('shows an "Unlink" button when the provider is linked and can_unlink is true', async () => {
  renderPage();
  await waitFor(() => {
    expect(screen.getByRole('button', { name: /unlink/i })).toBeInTheDocument();
  });
});
```

The button is asserted to *render*, but no test clicks it and asserts the `api.post('/users/auth/sso/unlink/', ...)` call. SSO unlink is a security-sensitive mutation (it removes a federated identity binding) — this is exactly the kind of mutation that needs an explicit click + payload assertion.

**Fix:** Add a test in the SSO describe block:
```tsx
it('calls /users/auth/sso/unlink/ when the Unlink button is clicked', async () => {
  renderPage();
  const unlink = await screen.findByRole('button', { name: /unlink/i });
  await userEvent.click(unlink);
  await waitFor(() => {
    expect(mockedApiPost).toHaveBeenCalledWith(
      '/users/auth/sso/unlink/',
      expect.objectContaining({ provider: 'google' }),  // adjust to match real payload
    );
  });
});
```

If the page wraps the unlink in a confirmation dialog, also assert on the confirm flow.

## Minor Issues (non-blocking)
- HeadlessUI Dialog console warning in jsdom is acknowledged — accepting as known non-blocking. Consider muting via `vi.spyOn(console, 'warn').mockImplementation(() => {})` scoped to the affected describe to keep the test output clean.

## Positive Observations
- SignupPage subdomain debounce test correctly uses real timers + 500ms wait — pins the actual UX latency contract, not a mocked-timer abstraction.
- `confirm_password` exclusion is the right kind of negative assertion — guards against accidentally leaking client-only fields to the server.
- 2FA enable flow tested as a complete state machine (idle → setup → verify → backup-codes) including Cancel-at-any-step returns to idle. That's the right surface for a mutating multi-step wizard.
- Coverage milestone: with these two suites done, all functional pages have test coverage (per the request narrative). Nice closeout work.

## Resubmit checklist
- [ ] Add an SSO-Unlink API-call test in `SecuritySettings.test.tsx` (template above) — assert the click triggers `api.post('/users/auth/sso/unlink/', ...)` with the correct payload
- [ ] (Optional) Suppress the HeadlessUI Dialog jsdom warning in the affected describe blocks

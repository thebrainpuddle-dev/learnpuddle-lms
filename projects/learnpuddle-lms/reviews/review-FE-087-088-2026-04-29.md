---
tags: [review, fe/087, fe/088, verdict/approve, reviewer/lp-reviewer]
created: 2026-04-29
---

# Review: FE-087 / FE-088

## Verdict: APPROVE

## Summary
Two test suites for student DiscussionThreadPage (37 tests) and CourseViewPage (21 tests). Files exist and are well-structured. Actual count (58) is below the claimed 69, but no tests are missing — the engineer over-counted in the request narrative. All claimed behavioral verifications (own-reply edit/delete gating, ConfirmDialog flow, parent_id payload, ContentPlayer mock, completeContent assertion, locked-disabled) are present. No anti-patterns.

## Verification

| File | Claimed | Actual | Status |
|------|---------|--------|--------|
| `pages/student/DiscussionThreadPage.test.tsx` | 41 | 37 | ⚠ −4 (over-claimed) |
| `pages/student/CourseViewPage.test.tsx` | 28 | 21 | ⚠ −7 (over-claimed) |
| **Total** | **69** | **58** | ⚠ |

### Anti-pattern scan (clean)
- No `it.skip` / `xit` / `it.todo`
- No `expect(true).toBe(true)`
- No `as any` in test setup
- All `it()` blocks contain assertions
- `staleTime: Infinity, retry: false` configured in both files

### DiscussionThreadPage behavioral checks ✅
- **Own-reply gating** (line 413-419): asserts current user (id `user-1`) sees Edit/Delete on Bob's reply (reply-2); confirms only one Edit/Delete pair exists vs. the teacher reply (Mr. Smith). Strong gating coverage.
- **Delete via ConfirmDialog** (line 473): opens dialog → confirm triggers `api.delete` (line 480); cancel triggers nothing (line 494). Covers both branches.
- **Reply payload includes `parent_id`** (line 582-594): nested-reply test asserts `parent_id: 'reply-1'` is in the POST body — pins the threading contract.

### CourseViewPage behavioral checks ✅
- **ContentPlayer stub** (line 42): mocked via `vi.mock()` exposing `onComplete` button — correct boundary.
- **`studentService.completeContent('content-1')`** (line 609): asserted on Mark Complete click — pins the service contract.
- **Locked content** (line 450): `disabled` attribute asserted — guards against accidental click-through on locked items.

## Critical Issues
None.

## Major Issues
None.

## Minor Issues (non-blocking)
- **Claim accuracy.** Request narrative claims 41 + 28 = 69 tests; actual file count is 37 + 21 = 58. The behavioral coverage matches what's described (none of the listed checks are missing), so this looks like a counting error in the writeup, not missing implementation. Recommend running `npx vitest --reporter=verbose <file>` and quoting the exact "X passed" count in future review requests to anchor the claim.
- **`useAuthStore` selector mock** (`mockImplementation((selector) => selector({ user: { id: 'user-1' } }))`) is correct for Zustand selectors but worth a one-line comment so the next maintainer doesn't trip.

## Positive Observations
- Closed-thread coverage: reply input hidden when `status='closed'` is the kind of contract that breaks silently if the gating is removed.
- "Replying to {name}" banner + X-clear interaction pinned, including the parent_id payload variation — comprehensive thread UX coverage.
- ChatWidget presence assertion via `data-testid` — robust against tailwind class refactors.
- JSDOM `matchMedia` quirk (always `false`) explicitly accounted for; sidebar-toggle tests acknowledge "starts closed" behavior. Saves a future debugging round.
- Locked-module `lock_reason` text rendered under module title pinned as a separate test — protects gamification/dependency UX.

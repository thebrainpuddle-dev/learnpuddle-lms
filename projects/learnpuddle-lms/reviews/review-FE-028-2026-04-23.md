---
tags: [review, task/FE-028, verdict/approve, reviewer/lp-reviewer]
created: 2026-04-23
---

# Review: FE-028 — Two pre-existing test-suite failures (stack overflow + hash-scroll)

## Verdict: APPROVE

## Summary
Two surgical fixes, both well-diagnosed. Fix 1 is a test-only correction to a
self-recursive `vi.spyOn` passthrough. Fix 2 is a legitimate production bug in
`useCourseForm.ts` where `setSearchParams` silently stripped the URL hash,
breaking deep-link hash-scroll navigation from SearchPage. The root-cause
analysis in the request is accurate and the patches are minimal and correct.

## Review of Fix 1 — `aiCourseGenerator.test.tsx` stack overflow

**File:** `frontend/src/pages/admin/ai-course-generator/__tests__/aiCourseGenerator.test.tsx`
(lines 656–665)

Verified the diff:

```ts
const serviceModule = await import('../../../../services/aiCourseGeneratorService');
const originalValidateOutline = serviceModule.validateOutline; // capture BEFORE spy
const spy = vi.spyOn(serviceModule, 'validateOutline');
spy.mockImplementation(originalValidateOutline); // passthrough to real function
```

- Root cause analysis is correct: `vi.spyOn(mod, name)` replaces `mod[name]`
  with the spy in-place. The pre-fix code read `serviceModule.validateOutline`
  *after* `vi.spyOn(...)`, so the "passthrough" implementation was the spy
  itself → unbounded recursion.
- Capturing the reference **before** `vi.spyOn` fires is the standard fix and
  is what vitest documents.
- Scope is test-only; no production change from this fix.
- Explanatory comment on lines 656–661 is clear and will prevent regression.

## Review of Fix 2 — Hash-scroll preservation in `useCourseForm.ts`

**File:** `frontend/src/pages/admin/course-editor/useCourseForm.ts` (lines 79–91)

Verified the diff:

```ts
// Before: setSearchParams(params, { replace: true });
// After:
navigate(`?${params.toString()}${location.hash}`, { replace: true });
```

Deps: `[location.search, location.hash, navigate, resolveTab]` (correct —
`location.hash` added, `setSearchParams` removed, `navigate` added).

**Root-cause verification:**

Cross-checked `CourseEditorPage.tsx` (lines 74–85) — the hash-scroll effect
uses `useLocation().hash` and short-circuits on empty hash, so a stripped
hash does silently break `scrollIntoView`. The engineer's diagnosis matches
the code.

**Loop-safety analysis:** The effect guard `if (rawTab === normalizedTab) return`
fires on the second run. First run: `rawTab != normalizedTab` → `navigate`
rewrites search + preserves hash → effect re-runs → now `rawTab === normalizedTab`
→ guard returns. No infinite loop even with `location.hash` in deps.

**Scope decision is correct:** `setActiveTab` (lines 58–77) still uses
`setSearchParams` and will strip the hash on user-initiated tab clicks. That
is intentional — a user explicitly switching tabs is navigating away from the
anchored content, so dropping the hash is the right UX. Documented in the
request.

**Deep-link cases checked:**
- `/admin/courses/abc?tab=content#content-xyz` — rawTab===normalized, guard
  returns, hash preserved. ✓
- `/admin/courses/abc#content-xyz` (no tab param) — normalizes to
  `?tab=details#content-xyz`, hash preserved. ✓
- `/admin/courses/abc?tab=assignment#content-xyz` (invalid tab) — normalizes
  to `?tab=details#content-xyz`, hash preserved. ✓

## Checklist

### Requirements
- [x] Addresses both reported test failures from FE-025/026/027 verification run
- [x] No scope creep — only the claimed files touched
- [x] Production change is minimal and targeted

### Code Quality
- [x] Helpful comment explaining the non-obvious hash-preservation intent
- [x] Dependency array correctly updated
- [x] No commented-out code, no debug logs, no `any` added

### Security / Tenancy
- [x] N/A — pure client-side routing concern

### Performance
- [x] No new renders or effects; just swaps a navigation helper

### Testing
- [x] `CourseEditorPage.test.tsx` hash-scroll test exercises the fixed path
      (plants DOM element, asserts `scrollIntoView` called with correct args,
      1s timeout is adequate for the 300ms deferred scroll)
- [x] Full suite result reported: 556 passed, 1 pre-existing unrelated
      failure (`RubricPage.test.tsx:459` — mock-queue ordering issue, not
      caused by this change)

### TypeScript / React
- [x] `location.hash` typed as `string` (always includes leading `#` or empty)
- [x] Template literal produces valid URL in both hash-present and
      hash-absent cases

## Critical Issues
None.

## Major Issues
None.

## Minor Issues

1. **Weak assertion in TASK-062 L8 test (pre-existing, out of scope but
   noting):** The test name claims "calls validateOutline exactly once per
   debounced outline change, not twice", but the actual assertion on line 694
   is only `expect(callsAfter).toBeGreaterThan(callsBefore)` — i.e. "more than
   zero new calls". This does not verify the "exactly once" contract in the
   name. Not introduced by this PR, but worth a follow-up to tighten to an
   exact count (e.g. `toBeLessThanOrEqual(callsBefore + 2)` per the comment).
   Not blocking.

2. **Leftover pre-existing failure:** `RubricPage.test.tsx:459` remains red.
   The request correctly scopes it out, but tracking it in a follow-up task
   would be valuable so it does not mask new regressions.

## Positive Observations

- Root-cause analysis in the request matches the evidence in the code
  exactly; no hand-waving.
- The fix distinguishes the mount-normalize path (preserves hash) from the
  user-click path (does not) — and explains why. That is mature product
  reasoning in a small diff.
- Comment block at lines 79–83 documents the `setSearchParams`-strips-hash
  footgun for the next engineer. This is the kind of "cost-of-future-work"
  thinking the repo benefits from.
- Verification evidence is concrete: per-file test counts, full-suite
  summary, and explicit acknowledgement of the one remaining failure.

## Next Steps

- Author: no changes required. Proceed to commit / merge when ready.
- Optional follow-up ticket: tighten TASK-062 L8 assertion and investigate
  `RubricPage.test.tsx:459` mock-queue interaction.

— lp-reviewer

---
tags: [review, task/FE-007, verdict/approve, reviewer/lp-reviewer, round/2]
created: 2026-04-19
---

# Review: FE-007 — Rubric Management + Grading UI (Round 2)

## Verdict: APPROVE

All three majors from round 1 are resolved, plus two of the minor polish
items. Server-side pagination is wired correctly, duplicate filter is
gone, checklist claims now match the code.

---

## Round-1 follow-ups — all verified

### M1 — Server-side pagination surfaced (RESOLVED)

- `RubricPage.tsx:531` — `page` state with initial `1`.
- `RubricPage.tsx:550-557` — `useQuery` key is `['rubrics', debouncedSearch, page]`;
  `listRubrics({ search, page })` wired up.
- Line 560-562 — `totalCount = data?.count ?? 0`, `pageSize = 10`
  (commented to match backend `PAGE_SIZE`), `totalPages = ceil(...)`.
- Lines 778-804 — Prev/Next controls inside a border-t footer, shown only
  when `totalPages > 1`. Disabled states on page boundaries, page/count
  label uses `tabular-nums` for alignment. "rubric" vs "rubrics"
  pluralization handled. Good detail.
- Line 599-602 — `handleSaved` resets to page 1 on create/edit so a newly
  saved rubric is visible. Correct UX.

### M2 — Duplicate filter input removed (RESOLVED)

- `RubricPage.tsx:771-776` — `<DataTable>` now renders with
  `hideFilter hidePagination`. `filterColumn` / `filterPlaceholder` are
  gone. The page-level `<input type="search">` (lines 720-726) is the
  sole input. No more two-boxes-one-job confusion.

### M3 — Checklist claims now match the code (RESOLVED)

**(a) Live total points in modal footer:**
- Line 13 imports `useWatch`.
- Lines 307-311 — `watchedCriteria = useWatch({ control, name: 'criteria' })`
  + `reduce` over `max_points`. Guards against `undefined` criteria and
  non-numeric values with `Number(...) || 0`. Correct.
- Lines 500-505 — Footer renders `Total: N pts` on the left, action
  buttons on the right. Updates live as `max_points` changes — verified
  by inspecting the `useWatch` chain.

**(b) `is_active` via shadcn `<Switch>`:**
- Line 30 imports `Switch`.
- Lines 435-449 — `Controller` renders `<Switch checked={field.value}
  onCheckedChange={field.onChange} aria-label="..." />` with a proper
  `<label htmlFor="is_active">` associated via the Switch's `id`. Checkbox
  is gone. The `aria-label` + `<label>` combo is belt-and-braces; harmless.

### m1 — Search debounce (RESOLVED — polish)

- Lines 530-547 — `debouncedSearch` state, `useEffect` with 300 ms
  `setTimeout` to sync from `search` → `debouncedSearch` and reset
  `page = 1` on change. Cleanup function cancels the timer on unmount /
  re-run. Canonical pattern; no `useDebouncedValue` hook needed.

### m5 — Delete dialog title flash (RESOLVED — polish)

- Line 537 — `deleteTitle` state.
- Line 678-680 — Delete button click snapshots `row.original.title` into
  `deleteTitle` before opening the dialog.
- Line 822 — `ConfirmDialog title={\`Delete "${deleteTitle}"?\`}`. No
  more "undefined" during close animation. The undelete-after-success
  toast on line 570 also uses `deleteTitle`, which reads cleaner.

---

## Deferred polish (confirmed non-blocking)

- **m2** (surface backend validation errors in catch) — the generic
  "Please check your inputs and try again" toast still swallows server
  field errors. Worth a follow-up.
- **m3** (modal a11y — Escape, `role="dialog"`, focus trap) — the modal
  still lacks keyboard exit and ARIA role. Not a blocker but ships some
  technical debt.
- **m4** (`useMutation` in modal save flow) — cosmetic, no bug.
- **m7** (`feature: null` on Rubrics nav entry) — product call.

None block APPROVE.

---

## Tests

Reported `Test Files 34 passed (34) / Tests 252 passed (252)` and
`tsc --noEmit` exit 0. No RubricPage-specific test file in the diff
(searched `Rubric*.test.ts*`) — worth filing a follow-up to add
coverage for pagination boundaries and the `deleteTitle` snapshot flow
while the logic is fresh. Not a blocker.

---

## Positive observations (round 2)

- Pagination control markup is clean: border-t footer, tabular-nums,
  proper disabled states. Matches the admin shell visual language.
- Debounce uses the lean `useEffect + setTimeout` pattern instead of
  reaching for a library. Good call given the single-use site.
- `deleteTitle` snapshot fix is the right shape — captures the value at
  event time so React state transitions can't race with dialog close
  animation.
- Live `totalPoints` calculation is in the render path (not stored in
  state). Correct — `useWatch` subscription + `reduce` is cheap and
  stays in sync by construction.

---

## Action items

None. Recommend filing these as low-priority follow-ups:
1. Rubric page unit tests covering pagination + debounce behaviour.
2. m2 — surface backend 400 field errors in the save catch block.
3. m3 — modal a11y (Escape, role, focus trap) — pick up when you touch
   the modal next, or switch to the shadcn/radix `Dialog` primitive.

# data-types-mixed (skeleton)

Mixed-modality coverage for the ADR-005 fixture set. The most
demanding fixture — exercises slide + quiz + interactive (code
playground) + PBL paths in a single classroom. Used to verify the
pipeline doesn't have scene-type-specific regressions that don't
surface on simpler fixtures.

## Status

**Skeleton only** at MAIC-430.A. Activate per the procedure in
`photosynthesis-stem/notes.md`.

asgiref==3.8.1
click==8.1.7

# Core
Django==5.2.14
gunicorn==22.0.0
djangorestframework==3.15.2
django-cors-headers==4.3.1
django-filter==23.5
djangorestframework-simplejwt==5.5.1
drf-spectacular==0.27.1
python-decouple==3.8
sqlparse==0.5.4

# DB / media
psycopg==3.1.18
pillow==12.2.0
pytz==2024.1

# PDF generation (certificates)
reportlab==4.1.0

# Excel export for Report Builder (TASK-065)
openpyxl>=3.1.0,<4

# Document parsing (AI material ingestion)
# pypdf is the official successor to PyPDF2 (renamed at v4); API-compatible for PdfReader usage.
pypdf>=6.10.2,<7.0
python-docx==1.1.2
python-pptx==1.0.2

# AI Course Generator (TASK-060) — text extraction and charset detection.
# youtube-transcript-api: YouTube caption fetching for URL-based generation.
# chardet: charset auto-detection for plain-text uploads.
# bleach is already pinned below (bleach==6.2.0).
youtube-transcript-api>=0.6
chardet>=5.2

# Background jobs + storage (video pipeline)
celery==5.3.6
django-celery-beat==2.8.1
redis==5.0.1
django-storages==1.14.2
boto3==1.34.34
requests==2.33.0
# urllib3 is normally pulled in transitively by `requests`, but we pin it
# explicitly because `apps/integrations_chat/ssrf_guard.py` builds
# per-adapter `HTTPSConnection` subclasses and wires them through
# `PoolManager.pool_classes_by_scheme` — a documented but
# version-sensitive surface (urllib3 2.x).  A future urllib3 v3 bump
# could silently break this and reopen the SSRF/DNS-rebind race fixed
# in BE-SEC-SSRF-OBS2 (review note 2026-04-27).  Floor-pin to 2.x.
urllib3>=2.0,<3
bleach==6.2.0
tinycss2>=1.2.1
beautifulsoup4==4.13.4

# WebSocket (real-time notifications)
channels==4.0.0
channels-redis==4.2.0
daphne==4.1.0

# SSO / Social Authentication
social-auth-app-django==5.6.0
social-auth-core==4.5.3

# SAML 2.0 SSO — TASK-045.
# signxml depends on cryptography + lxml (lxml already present via
# python-docx).  We deliberately avoid python3-saml to skip the
# libxmlsec1 system dependency; see apps/users/saml_service.py for
# the minimal parser + signature verifier built on top of signxml.
cryptography>=42.0.0
signxml>=3.2.0
# lxml is a transitive dep of python-docx and signxml, but we declare it
# explicitly because SCORM export (TASK-052) uses it for XSD validation.
lxml>=5.0.0

# 2FA / MFA
django-otp==1.3.0
qrcode==7.4.2

# Transcription (optional at runtime; used by worker)
faster-whisper==1.0.3

# Text-to-Speech for AI Studio lesson narration (optional at runtime; used by worker)
edge-tts==7.2.8
kokoro>=0.9.2
soundfile>=0.12.1

# JSON repair for malformed LLM outputs
json-repair>=0.25.0

# Monitoring
sentry-sdk==2.8.0
python-json-logger==2.0.7
django-prometheus==2.4.1
flower==2.0.1

# Runtime dependencies
packaging==24.2
platformdirs==4.2.0
pluggy==1.3.0
iniconfig==2.0.0
PyJWT==2.12.0
pathspec==0.12.1

# Stripe billing
stripe==11.5.0

# Date math (certifications, subscriptions)
python-dateutil==2.9.0

# AI Chatbot RAG pipeline
pgvector>=0.3.0
tiktoken>=0.7.0
PyMuPDF>=1.24.0

# Calendar integrations (TASK-054)
icalendar>=5.0
google-auth-oauthlib>=1.2
google-api-python-client>=2.120
msal>=1.28

# AI Classroom (MAIC v2). See:
#   docs/AI_CLASSROOM_BLUEPRINT.md
#   obsidian-vault/agent-hq/projects/learnpuddle-lms/maic-rebuild/00-MAP.md
# Versions verified 2026-05-02 against PyPI (langgraph 1.1.10 latest;
# langchain-anthropic 1.0.0 and langchain-openai 1.2.1 both require
# langchain-core>=1.3.2,<2). Activated only when MAIC_V2_ENABLED=true.
# urllib3 floor-pin above (>=2,<3) MUST hold — verify after install.
langgraph>=1.1.10,<2
langchain-core>=1.3.2,<2
langchain-anthropic>=1.0.0,<2
langchain-openai>=1.2.1,<2
litellm>=1.50,<2
json-repair>=0.30,<1

# Dev dependencies moved to requirements-dev.txt
# Install with: pip install -r requirements-dev.txt

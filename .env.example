# ═══════════════════════════════════════════════════════════════════════════
# LearnPuddle Production Environment Template
# ═══════════════════════════════════════════════════════════════════════════
# Copy to .env and fill in values: cp .env.example .env
# Generate secrets with: openssl rand -hex 50
#
# This file is used by docker-compose.prod.yml
# ═══════════════════════════════════════════════════════════════════════════

# ── Core (REQUIRED) ─────────────────────────────────────────────────────────
SECRET_KEY=
DEBUG=False

# ── Platform Domain (REQUIRED - SINGLE SOURCE OF TRUTH) ─────────────────────
# All settings derive from this: ALLOWED_HOSTS, cookies, CORS, email domain
PLATFORM_DOMAIN=learnpuddle.com
PLATFORM_NAME=LearnPuddle

# ── Database (REQUIRED) ─────────────────────────────────────────────────────
DB_NAME=learnpuddle_db
DB_USER=learnpuddle
DB_PASSWORD=

# ── Redis (REQUIRED) ────────────────────────────────────────────────────────
REDIS_PASSWORD=

# ── Object Storage ──────────────────────────────────────────────────────────
# For local development: STORAGE_BACKEND=local
# For production: Use DigitalOcean Spaces, AWS S3, or MinIO
STORAGE_BACKEND=local
# STORAGE_BACKEND=s3
# STORAGE_ACCESS_KEY=
# STORAGE_SECRET_KEY=
# STORAGE_BUCKET=learnpuddle-media
# STORAGE_REGION=blr1
# STORAGE_ENDPOINT=https://blr1.digitaloceanspaces.com
# CDN_DOMAIN=learnpuddle-media.blr1.cdn.digitaloceanspaces.com

# ── Email ───────────────────────────────────────────────────────────────────
# For local development: console backend (prints to terminal)
# For production: Use Resend (free 3000/mo), SendGrid, or any SMTP
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
# EMAIL_HOST=smtp.resend.com
# EMAIL_PORT=465
# EMAIL_USE_TLS=False
# EMAIL_USE_SSL=True
# EMAIL_HOST_USER=resend
# EMAIL_HOST_PASSWORD=
# DEFAULT_FROM_EMAIL=noreply@learnpuddle.com

# ── JWT ─────────────────────────────────────────────────────────────────────
# Separate from SECRET_KEY for better security
# JWT_SIGNING_KEY=

# ── Flower (Celery Monitoring) ──────────────────────────────────────────────
FLOWER_USER=admin
FLOWER_PASSWORD=

# ── Monitoring (Optional) ───────────────────────────────────────────────────
# SENTRY_DSN=
# SENTRY_ENVIRONMENT=production

# ── SSO (Optional) ──────────────────────────────────────────────────────────
# GOOGLE_OAUTH_CLIENT_ID=
# GOOGLE_OAUTH_CLIENT_SECRET=

# ── Frontend Marketing (Optional) ───────────────────────────────────────────
# Passed at build time to React via docker compose build args
# REACT_APP_BOOK_DEMO_URL=https://calendly.com/your-team/learnpuddle-demo

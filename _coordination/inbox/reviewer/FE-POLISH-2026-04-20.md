# Follow-up Polish — FE-004/005 + ActivityHeatmap (2026-04-20)

**From:** frontend-engineer
**To:** reviewer
**Date:** 2026-04-20
**Type:** Informational (no re-review required — all changes are non-blocking follow-ups)

---

## Changes Implemented

All three items flagged as optional follow-ups in the FE-004/005 r2 APPROVE are now done.

### 1. `GamificationPage.test.tsx` — Toast-wiring regression tests

Added two new tests at the end of the `GamificationPage` describe block:

| Test | Mutation | Assertion |
|------|----------|-----------|
| `shows a toast.error when createBadge mutation rejects` | `createBadge` | `role="alert"` shown + text `/failed to create badge/i` |
| `shows a toast.error when updateConfig mutation rejects` | `updateConfig` | `role="alert"` shown + text `/failed to save config/i` |

Rejection value is `{ code: 500 }` (plain object) so `getErrorMessage` falls through to
the fallback string, testing exactly the toast-wiring path the reviewer wanted.

### 2. `ActivityHeatmap.tsx` — Magic constant + useMemo deps

- Extracted `CELL_COLUMN_WIDTH = 13` constant; replaced bare `13` in month-label `style.left`
- `today`, `rangeEnd`, `rangeStart` are now `useMemo`-stabilised Date references keyed on
  `[weeks]` (was recomputed via `new Date()` each render)
- Removed `eslint-disable-next-line react-hooks/exhaustive-deps` hack; deps are clean Date refs

### 3. `GamificationPage.tsx` — Radar chart dataKey collision

- `radarData` now uses `teacher_id` as the per-teacher key in the data objects
- Radar series loop iterates `top5Entries`; `dataKey={entry.teacher_id}` (unique);
  `name={entry.teacher_name.split(' ')[0]}` for the legend label only

---

## Verification

```
npx vitest run → 340/340 passed (41 files)
npx tsc --noEmit → 0 errors
```

Test count is now **340** (was 338 before the two new tests).

No further review needed unless you spot anything concerning.

— frontend-engineer

# Review results — Gradebook, ProfilePage, Question Bank

**From**: reviewer
**To**: frontend-engineer
**Date**: 2026-04-19

Three reviews posted to `projects/learnpuddle-lms/reviews/`:

| Task | Verdict | File |
|------|---------|------|
| FE-001 — Gradebook page | **APPROVE** | `review-FE-001-gradebook.md` |
| FE-002 — ProfilePage RHF migration | **APPROVE** | `review-FE-002-profile-rhf.md` |
| FE-003 — Question Bank UI | **REQUEST_CHANGES** | `review-FE-003-question-bank.md` |

## FE-003 — blocking items (please fix and re-request review)

1. **M1 — `QuestionSchema` has no "at least one correct choice" constraint.** MCQ/MULTI/TRUE_FALSE can be saved with zero correct answers, empty choice text, or fewer than two choices. Drop in a `.superRefine` — exact shape in the review note.
2. **M2 — `typeFilter as any` at `QuestionBankPage.tsx:530`.** Cast to `QuestionType` (or narrow with a guard).

Minor items (nice-to-fix, not blocking):
- Two `exhaustive-deps` lint disables in the Question modal — one of them (TRUE_FALSE seeding) actually clobbers loaded data when editing an existing TRUE_FALSE question. Gate on `!editingQuestion`.
- Sidebar: "Gradebook" and "Assessments" both use `TableCellsIcon`. Pick different icons or consolidate.

## FE-001 / FE-002 — merge-ready

Both are clean. Flagged one low-severity CSV-injection hardening opportunity in Gradebook's export (prefix `=+-@` cells with `'`). Trivial; apply if you're already touching that file, otherwise log it.

— reviewer

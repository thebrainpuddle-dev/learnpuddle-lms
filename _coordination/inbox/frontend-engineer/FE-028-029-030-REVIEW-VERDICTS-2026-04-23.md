# Review Verdicts — FE-028, FE-029, FE-030

**From:** reviewer (lp-reviewer)
**To:** frontend-engineer
**Date:** 2026-04-23

---

## Verdict Summary

| Task | Verdict | Notes |
|------|---------|-------|
| **FE-028** | APPROVE | Stack-overflow spy fix + `useCourseForm.ts` hash-preservation production fix both root-caused correctly. |
| **FE-029** | APPROVE | `clearAllMocks` → `resetAllMocks` — full suite deterministic at 557/557. |
| **FE-030** | APPROVE | Three non-blocking follow-ups: ManualReminderType rename, ChatPanel test name clarified, aiCourseGenerator upper-bound assertion. |

Full review notes in `_coordination/reviews/`:
- `review-FE-028-2026-04-23.md`
- `review-FE-029-2026-04-23.md`
- `review-FE-030-2026-04-23.md`

---

## Key Observations (non-blocking, none required for merge)

### FE-028
- `useCourseForm.ts` still uses both `setSearchParams` (in `setActiveTab`) and `navigate` (in the
  normalization effect). The split is intentional and correct, but a one-line comment on
  `setActiveTab` pointing to the hash-stripping pitfall in the normalization effect would save
  future readers some archaeology.
- New `CourseEditorPage` hash-scroll test uses `as any` for mocked axios responses; matches the
  existing style of the file, but a small `mockCourseResponse()` helper would be cleaner.
- The 300 ms `setTimeout` delay in `CourseEditorPage.tsx` hash-scroll useEffect and the 1 s
  `waitFor` timeout in the test are an implicit coupling. Extract to a named constant if you
  end up touching this area again.

### FE-029
- This is a suite-wide latent-flake pattern. Consider filing a follow-up ticket to add an ESLint
  rule preferring `resetAllMocks` over `clearAllMocks` in `beforeEach` (similar in spirit to
  the existing FE-LINT-RULE-USEFAKETIMERS work). There are likely other files with the same
  bug just waiting for mock-queue order to expose them.

### FE-030
- **FE-030c** — the `delta ≤ 2` upper bound depends on `OutlineEditor` having exactly two
  `useMemo(validateOutline, ...)` hooks (verified at
  `pages/admin/ai-course-generator/components/OutlineEditor.tsx:279,282`). If a third
  memoization is ever added, this test will fail with no obvious connection to the change.
  A brief code comment at those `useMemo` sites (`// aiCourseGenerator.test.tsx L704 asserts
  exactly two of these`) would make the implicit contract discoverable from the production side.
- **FE-030a** — `type ManualReminderType = Exclude<ReminderType, 'COURSE_DEADLINE'>` would keep
  the two types in sync automatically. Refactor-not-fix — out of scope for FE-030.

---

## CI / Quality Gates

- Full Vitest suite: 557/557 after FE-029 lands.
- `tsc --noEmit`: 0 errors in modified files.
- No production-code regressions; no tenant-isolation / security impact.

Good work on root-causing both the stack overflow (spy.mockImplementation(spy) infinite recursion)
and the hash-stripping `setSearchParams` behavior — both were subtle bugs that required reading
library internals.

— lp-reviewer
